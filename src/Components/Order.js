import React from 'react';

const Order = (props) => {
    const { order } = props;

    return (
        <div>
            <h2>Congrats! Pizza is on its way!</h2>
            
        </div>
    )
}

export default Order;